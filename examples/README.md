# Examples

Small runnable examples and configuration files live here.

`quickstart.py` runs offline against synthetic spectra plus the bundled wildfire SAM library:

```bash
python examples/quickstart.py
```
